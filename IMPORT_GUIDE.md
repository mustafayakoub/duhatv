# 📦 دليل تصدير واستيراد البيانات
# Data Export & Import Guide

---

## 🎯 المشكلة

لديك قاعدة بيانات كبيرة (200 MB) لا يمكن رفعها مباشرة.

**الحل:** تصدير البيانات إلى ملفات JSON أصغر يمكن رفعها بسهولة.

---

## 📋 الخطوات

### 🔵 الخطوة 1: التصدير (على جهازك - Windows/Linux/Mac)

#### 1.1 تحميل السكريبت

حمّل الملف: **`export_quran_database.py`**

#### 1.2 ضع السكريبت مع قاعدة البيانات

```
📁 مجلد العمل
  ├── export_quran_database.py       ← السكريبت
  └── quran_ultimate_final.db        ← قاعدة بياناتك
```

أو عدّل السكريبت ليشير إلى المسار الصحيح:

**على Windows:**
```python
SOURCE_DB = r"C:\quran9\quran_ultimate_final.db"
```

**على Linux/Mac:**
```python
SOURCE_DB = "/home/user/quran9/quran_ultimate_final.db"
```

#### 1.3 تشغيل السكريبت

**على Windows:**
```cmd
python export_quran_database.py
```

**على Linux/Mac:**
```bash
python3 export_quran_database.py
```

#### 1.4 اختر نوع التصدير

سيسألك السكريبت:

```
اختر نوع التصدير:
1. تصدير عينة صغيرة للاختبار (سريع - موصى به أولاً)
2. تصدير البيانات الكاملة (قد يستغرق وقتاً)
3. كلاهما

اختيارك (1/2/3) [افتراضي: 1]:
```

**الموصى به:**
- ابدأ بـ **`1`** لتصدير عينة صغيرة (سريع - للاختبار)
- بعد التأكد من عمل كل شيء، شغّل مرة أخرى واختر **`2`** للبيانات الكاملة

#### 1.5 النتيجة

سيتم إنشاء مجلد **`quran_export`** يحتوي على:

```
📁 quran_export/
  ├── 01_schema.sql              ← بنية قاعدة البيانات
  ├── sample_data.json           ← عينة للاختبار (صغير)
  ├── surahs_info.json           ← معلومات السور
  ├── quran_text_part001.json    ← نصوص القرآن (جزء 1)
  ├── quran_text_part002.json    ← نصوص القرآن (جزء 2)
  ├── ...
  ├── tafsir_muyassar_part001.json
  └── README.txt                 ← تعليمات
```

**الملفات الكبيرة تُقسم تلقائياً إلى أجزاء < 2 MB**

---

### 🟢 الخطوة 2: رفع الملفات

#### خيار 1: رفع العينة أولاً (موصى به)

ارفع فقط:
- ✅ `sample_data.json` (صغير جداً - عدة KB)

**لماذا؟**
- اختبار سريع
- تأكد من عمل كل شيء
- ثم ارفع البيانات الكاملة

#### خيار 2: رفع كل شيء

ارفع كل محتويات مجلد **`quran_export`**

---

### 🟡 الخطوة 3: الاستيراد (على الخادم)

بعد رفع الملفات، شغّل سكريبت الاستيراد:

```bash
python3 import_quran_data.py
```

#### ماذا يحدث؟

1. ✅ يحذف قاعدة البيانات القديمة
2. ✅ ينشئ قاعدة بيانات جديدة
3. ✅ يستورد جميع البيانات
4. ✅ يتحقق من صحة الاستيراد

#### إذا رفعت العينة فقط:

سيسألك:
```
اختر نوع الاستيراد:
1. استيراد العينة فقط (سريع - للاختبار)
2. استيراد البيانات الكاملة

اختيارك (1/2) [افتراضي: 2]:
```

اختر **`1`** للعينة.

---

### 🟣 الخطوة 4: اختبار التطبيق

```bash
python3 quran_app_ultimate_final_v4.py
```

**سيفتح التطبيق الكامل مع البيانات المستوردة!** 🎉

---

## 📊 تفاصيل التصدير

### ما الذي يُصدّر؟

| الجدول | المحتوى | الحجم المتوقع |
|--------|---------|---------------|
| `surahs_info` | 114 سورة | < 10 KB |
| `quran_text` | 6,236 آية | ~ 5-10 MB |
| `quran_tajweed` | 6,236 آية | ~ 10-20 MB |
| `tafsir_muyassar` | 6,236 تفسير | ~ 20-40 MB |
| `tafsir_saadi` | 6,236 تفسير | ~ 30-50 MB |
| `tafsir_baghawi` | 6,236 تفسير | ~ 30-50 MB |
| `translation_english` | 6,236 ترجمة | ~ 5-10 MB |
| `translation_french` | 6,236 ترجمة | ~ 5-10 MB |
| `irab` | إعراب | متغير |
| `sarf` | صرف | متغير |

### التقسيم التلقائي

- الجداول الصغيرة: ملف واحد
- الجداول الكبيرة: تُقسم إلى أجزاء (كل جزء 1,000 سجل)

مثال:
```
quran_text_part001.json  (1,000 آية)
quran_text_part002.json  (1,000 آية)
...
quran_text_part007.json  (236 آية)
```

---

## 🔧 استكشاف الأخطاء

### ❌ خطأ: "no such table"

**الحل:**
```bash
# تأكد من استيراد البنية أولاً
python3 import_quran_data.py
```

### ❌ خطأ: "cannot open database file"

**الحل:**
```bash
# تأكد من الصلاحيات
chmod 644 quran_ultimate.db
```

### ❌ خطأ: "encoding error"

**الحل:**
- تأكد من أن الملفات بترميز UTF-8
- استخدم Python 3 (ليس Python 2)

### ❌ الملفات كبيرة جداً للرفع

**الحل:**

1. **صدّر العينة فقط:**
   ```bash
   python export_quran_database.py
   # اختر: 1
   ```

2. **أو قسّم يدوياً:**
   - افتح مجلد `quran_export`
   - ارفع ملف واحد في المرة
   - كرر حتى تنتهي

---

## 💡 نصائح

### ✅ ابدأ بالعينة

**لماذا؟**
- اختبار سريع (< دقيقة)
- تأكد من توافق البنية
- كشف المشاكل مبكراً

### ✅ افحص الملفات قبل الرفع

```bash
cd quran_export
ls -lh *.json
```

ابحث عن:
- ✅ أحجام منطقية
- ✅ لا يوجد ملفات فارغة (0 KB)

### ✅ احتفظ بنسخة احتياطية

```bash
# نسخ قاعدة البيانات الأصلية
cp quran_ultimate_final.db quran_backup.db
```

---

## 📈 سير العمل الموصى به

```
1. تصدير العينة (على جهازك)
   ↓
2. رفع sample_data.json
   ↓
3. استيراد العينة (على الخادم)
   ↓
4. اختبار التطبيق
   ↓
5. كل شيء يعمل؟ ✅
   ↓
6. تصدير البيانات الكاملة (على جهازك)
   ↓
7. رفع جميع ملفات JSON
   ↓
8. استيراد البيانات الكاملة (على الخادم)
   ↓
9. اختبار نهائي
   ↓
10. إطلاق التطبيق! 🚀
```

---

## 📞 ملاحظات مهمة

### حول التصدير:

- ✅ يحافظ على جميع البيانات (100%)
- ✅ يحافظ على الترميز العربي (UTF-8)
- ✅ يقسم الملفات الكبيرة تلقائياً
- ✅ يعمل على Windows/Linux/Mac

### حول الاستيراد:

- ✅ يتحقق من صحة البيانات
- ✅ يتعامل مع الأجزاء المتعددة تلقائياً
- ✅ يعرض تقرير مفصل
- ✅ يختبر الاستعلامات المعقدة

---

## 🎯 الخلاصة

### الطريقة السريعة (للاختبار):

```bash
# على جهازك
python export_quran_database.py
# اختر: 1 (عينة)

# ارفع: quran_export/sample_data.json

# على الخادم
python3 import_quran_data.py
# اختر: 1 (عينة)

# اختبار
python3 quran_app_ultimate_final_v4.py
```

### الطريقة الكاملة:

```bash
# على جهازك
python export_quran_database.py
# اختر: 2 (كاملة)

# ارفع: كل محتويات quran_export/

# على الخادم
python3 import_quran_data.py
# اختر: 2 (كاملة)

# تشغيل التطبيق
python3 quran_app_ultimate_final_v4.py
```

---

## ✨ مميزات السكريبتات

### export_quran_database.py

- 📊 تحليل تلقائي لقاعدة البيانات
- 📦 تقسيم ذكي للملفات الكبيرة
- 🎯 عينة اختبار سريعة
- 📝 تعليمات تلقائية

### import_quran_data.py

- 🔄 استيراد تلقائي للأجزاء المتعددة
- ✅ تحقق شامل من البيانات
- 📊 تقرير مفصل
- 🧪 اختبار استعلامات معقدة

---

**🌟 الآن لديك كل ما تحتاجه لنقل البيانات! 🌟**

*إذا واجهت أي مشكلة، راجع قسم "استكشاف الأخطاء" أعلاه*
